        lambdaGateway.addRoutes({
            path: '/{{lowerCase route}}/{id}',
            methods: [ HttpMethod.PUT ],
            integration: {{camelCase lambda}}DynamoIntegrations.put,
        });

        lambdaGateway.addRoutes({
            path: '/{{lowerCase route}}',
            methods: [ HttpMethod.POST ],
            integration: {{camelCase lambda}}DynamoIntegrations.post,
        });

        lambdaGateway.addRoutes({
            path: '/{{lowerCase route}}/{id}',
            methods: [ HttpMethod.DELETE ],
            integration: {{camelCase lambda}}DynamoIntegrations.del,
        });

        lambdaGateway.addRoutes({
            path: '/{{lowerCase route}}',
            methods: [ HttpMethod.GET ],
            integration: {{camelCase lambda}}DynamoIntegrations.get,
        });
